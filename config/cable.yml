# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  adapter: redis
  url: redis://:fusseo@pub-redis-17278.us-east-1-4.3.ec2.garantiadata.com:17278

development:
  adapter: async

test:
  adapter: async
